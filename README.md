Aaron Branch

⚠️ **IMPORTANT:**
For run_inference.py:
- Make sure columns of CSV are "Acceleration" and "Prediction"
- "Prediction" must be empty, as it's used as the output column
- Finally make sure the name of the input file is "input_data.csv"
